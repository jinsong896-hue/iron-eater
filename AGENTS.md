# Agent 开发规范

## 一、核心原则

> 每次收工前必须完成以下三项操作，缺一不可，顺序固定：

1.  **功能必须完整实现并实跑验证**：不能只写代码不跑。实跑必须有证据（日志/截图/录屏）。
2.  **进度必须落盘**：每天收工前更新进度文档。
3.  **任务必须拆解到可验证粒度**：每个任务有明确完成定义，有可验证子项。

## 二、文档规范

- 进度文档统一存放在进度目录（本仓库约定：`docs/progress/`）
- 文件名格式：PROGRESS_YYYYMMDD.md
- 每个功能条目必须包含完成情况和验证证据

## 三、验证规范

- 每个完成的条目附带验证证据
- 单测结果必须记录到进度文档

## 四、收工流程

> 收工前必须依次执行以下操作：

1.  更新进度文档（落盘）
2.  git add . && git commit -m "描述"（本地提交）
3.  git push（远程推送，如已配置）

## 五、异常处理

- 异常必须记录到进度文档：现象、原因、解决方案/待解决
- 未解决的异常标注“待解决”，纳入待办事项

## 六、任务清单

进度文档中维护任务清单区块（P0/P1/P2），记录待完成的跨会话任务。

## 七、素材使用规则

- 代码中需要美术/音频素材时，若项目内没有专用素材，优先使用免费素材（Godot 素材库 / Kenney 等 CC0 素材），后期再替换为正式美术。
- 该规则对历史代码同样生效：已有占位色块/临时素材在实现新功能时顺手替换为可用的免费素材，无法立即替换的登记到进度文档待办。
- 免费素材使用前先确认许可证（CC0 优先），并保留各素材包的 License 文件。

函数必须加注释