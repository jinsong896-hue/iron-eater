# Godot 4.7 Deltas — Per-Skill Inventory

Working document for the v1.11.0 alignment pass. Each row pairs a Godot 4.7 feature, behavior change, or stale-text fix with the skill(s) that should carry it, plus a verified source. Rows without a verified source are dropped.

## Research sources

- Engine changelog: `D:/Godot/godot/CHANGELOG.md` § "4.7 - 2026-06-18" (PR links)
- Migration guide: `D:/Godot/godot-docs/tutorials/migrating/upgrading_to_godot_4.7.rst`
- Class reference: `D:/Godot/godot-docs/classes/class_*.rst` (docs clone master, includes 4.7 API)
- Release page (for citations in skills): https://godotengine.org/releases/4.7/

**Verification rule:** a row enters this table only after its feature is confirmed in the class reference `.rst` or the migration guide (changelog PR link alone is acceptable for editor/tooling items with no class-reference surface). Kind is one of: `api` (new API section), `warn` (behavior-change warning), `fix` (stale-text fix).

Repo-wide minimum stays **Godot 4.3+**; every applied row is annotated `(Godot 4.7+)` or as a `Changed in Godot 4.7` warning.

---

## Inventory

| Skill | Kind | Feature | Source | New/updated section proposed |
|-------|------|---------|--------|------------------------------|
| physics-system | api | One-way collision direction — `CollisionShape2D` gains `one_way_collision_direction: Vector2 = Vector2(0, 1)` ("The direction used for one-way collision"), and `PhysicsServer2D.body_set_shape_as_one_way_collision()` gains an optional `direction: Vector2 = Vector2(0, 1)` parameter, so 2D one-way platforms can use a custom pass-through direction. | https://github.com/godotengine/godot/pull/104736 [done] | (update in existing section 5. Collision Shapes) |
| physics-system | warn | Jolt `SoftBody3D` mass/stiffness reinterpretation — with Jolt, `SoftBody3D` no longer defaults its mass to 0 (which meant 1 kg *per point*); it now defaults to 1 kg for the entire body, matching Godot Physics. `linear_stiffness` is also applied differently; `linear_stiffness` and `damping_coefficient` need re-tweaking after upgrade. | migration guide (GH-116041) [done] | (warning in existing section 11. SoftBody3D) |
| physics-system | warn | Jolt `WorldBoundaryShape3D.plane.d` sign flip — with Jolt, the plane distance `d` is now interpreted with the same convention as Godot Physics, i.e. the opposite sign compared to Godot 4.6; flip the sign to keep 4.6 behavior. | migration guide (GH-118948) [done] | (warning in existing section 8. Jolt Physics) |
| physics-system | warn | Jolt `Area3D` ↔ `SoftBody3D` overlaps — with Jolt, `Area3D` now reports overlaps with `SoftBody3D` from its signals and methods; configure collision layers/masks to ignore unwanted Area↔SoftBody interactions. | migration guide (GH-114198) [done] | (warning in existing section 4. Area2D/3D) |
| animation-system | warn | BlendSpace sync rework — `AnimationNodeBlendSpace1D/2D` replace the bool `sync` property (now deprecated) with a `sync_mode` `SyncMode` enum: `SYNC_MODE_NONE = 0` (default), `SYNC_MODE_INDEPENDENT = 1` (old `sync = true`), `SYNC_MODE_CYCLIC_MUTABLE = 2`, `SYNC_MODE_CYCLIC_CONSTANT = 3`; cyclic-constant uses the new `cyclic_length` property (must be > 0). AnimationTrees that blended correctly in 4.6 may need `sync_mode` set per blend space. | migration guide (GH-117275) [done] | (update/warning in existing section 5. AnimationTree — Canonical State Machine) |
| animation-system | api | Named blend points — `AnimationNodeBlendSpace1D/2D.add_blend_point()` gains an optional `name: StringName = &""` parameter, and blend point names/indices can be set and displayed in the editor. | https://github.com/godotengine/godot/pull/110369 [done] | (update in existing section 5. AnimationTree — Canonical State Machine) |
| animation-system | api | Ping-pong sprite playback — `SpriteFrames` gains a `LoopMode` enum (`LOOP_NONE = 0`, `LOOP_LINEAR = 1`, `LOOP_PINGPONG = 2`) with `set_animation_loop_mode(anim, loop_mode)` / `get_animation_loop_mode(anim)`; the old bool `set_animation_loop()` / `get_animation_loop()` are deprecated. Works with `AnimatedSprite2D/3D`. | https://github.com/godotengine/godot/pull/114556 [done] | (update in existing section 4. Sprite Frame Animation) |
| animation-system | warn | `LookAtModifier3D.relative` default flipped `true` → `false` — rotation is now applied relative to the rest pose by default instead of the current pose; set `relative = true` to restore 4.6 behavior. | migration guide (changed defaults, 4.7) [done] | (warning in existing section 6. Skeleton Modifiers (3D, 4.4+)) |
| tween-animation | api | `Tween.has_tweeners()` (const) — returns whether the Tween has any tweeners appended, useful for guarding reuse/kill logic on lazily built tweens. | https://github.com/godotengine/godot/pull/92429 [done] | (update in existing section 7. Tween Lifecycle) |
| input-handling | api | Joypad motion sensors — `Input.get_joy_accelerometer(device)` / `Input.get_joy_gyroscope(device)` (both `Vector3`), `has_joy_motion_sensors()`, `is_joy_motion_sensors_enabled()` / `set_joy_motion_sensors_enabled()`, `get_joy_motion_sensors_rate()`, plus calibration methods (`start/stop_joy_motion_sensors_calibration()`, `is_joy_motion_sensors_calibrated()`, `get/set_joy_motion_sensors_calibration()`, `clear_joy_motion_sensors_calibration()`). | https://github.com/godotengine/godot/pull/111679 [done] | (new subsection in existing section 5. Controller / Gamepad Support) |
| input-handling | api | Joypad vibration capability checks — `Input.has_joy_vibration(device)`, `get_joy_vibration_duration(device)`, `get_joy_vibration_remaining_duration(device)`, `get_joy_vibration_strength(device)` (`Vector2` weak/strong). | https://github.com/godotengine/godot/pull/114895 [done] | (update in existing section 5. Controller / Gamepad Support) |
| input-handling | warn | Mouse/keyboard device IDs changed from `0` to `InputEvent.DEVICE_ID_MOUSE = 32` and `InputEvent.DEVICE_ID_KEYBOARD = 16` (some joypads use ID 0). Code checking `event.device == 0` to detect keyboard/mouse breaks; compare against the constants or check the event type instead. | migration guide (GH-116274) [done] | (warning in existing section 9. Common Pitfalls) |
| input-handling | api | `VirtualJoystick` node — new `Control`-derived on-screen joystick with `action_up/down/left/right` (`StringName`, defaults `&"ui_up"` etc.), `joystick_mode`, `visibility_mode`, `deadzone_ratio = 0.0`, `clampzone_ratio = 1.0`, `joystick_size = 100.0`, `tip_size = 50.0`, `initial_offset_ratio = Vector2(0.5, 0.5)`, and StyleBox theme slots (`normal_joystick/tip`, `pressed_joystick/tip`). | https://github.com/godotengine/godot/pull/110933 [done] | (new subsection in existing section 6. Touch Input) |
| input-handling | api | New project setting `input_devices/joypads/ignore_joypad_on_unfocused_application = false` — when enabled, joypad input (including motion sensors) and LED changes are ignored and vibration is stopped while the app is unfocused. | https://github.com/godotengine/godot/pull/115119 [done] | (update in existing section 5. Controller / Gamepad Support) |
| input-handling | api | SDL misc gamepad buttons — `JoyButton` enum gains `JOY_BUTTON_MISC2 = 21` through `JOY_BUTTON_MISC6 = 25` for extra hardware buttons. | https://github.com/godotengine/godot/pull/116418 [done] | (update in existing section 5. Controller / Gamepad Support) |
| audio-system | warn | `AudioStreamPlayer2D/3D.area_mask` default changed `1` → `0` (disabled) — `Area2D/3D` `audio_bus_override` stops working for players left at the old default; set the mask back to layer 1 to restore bus overrides. (Migration guide says "AudioStreamPlayer", but the property only exists on the 2D/3D variants — class reference confirms default ``0`` on both.) | migration guide (GH-107679) [done] | (warning in existing section 4. Spatial Audio (2D & 3D)) |
| audio-system | api | `AudioStreamInteractive.TRANSITION_TO_TIME_PREVIOUS_POSITION = 2` (`TransitionToTime` enum) is now exposed to scripts — transition to the playback position the destination clip was previously at. | https://github.com/godotengine/godot/pull/114129 [done] | (update in existing section 8. Interactive & Adaptive Music (Godot 4.3+)) |
| gdscript-advanced | warn | Setting an element of a packed-array property (e.g. `obj.packed_prop[i] = x`) no longer calls the setter for the whole property — code relying on the setter firing for per-element writes silently breaks. | migration guide (GH-113228) [done] | (warning in existing section 8. Common pitfalls) |
| gdscript-advanced | api | New `CONFUSABLE_TEMPORARY_MODIFICATION` GDScript warning — flags modifying a temporary (discarded) value; controlled by project setting `debug/gdscript/warnings/confusable_temporary_modification = 1` (warn). | https://github.com/godotengine/godot/pull/118002 [done] | (update in existing section 8. Common pitfalls) |
| gdscript-patterns | warn | Typed-return inheritance — methods overriding a method with a typed return now inherit the return type, so overrides without an explicit `return` become errors; add `return null` (or a typed return) at the end of the override. | migration guide (GH-115763) [done] | (warning in existing section 1. Static Typing) |
| gdscript-patterns | warn | `type_exists()` global function deprecated — class reference marks it "Deprecated: Use `ClassDB.class_exists()` instead." | https://github.com/godotengine/godot/pull/116899 [done] | (update in existing section 10. Common Pitfalls) |
| particles-vfx | api | `ParticleProcessMaterial.particle_flag_inherit_emitter_scale = false` (`PARTICLE_FLAG_INHERIT_EMITTER_SCALE = 4`) — particles inherit the emitter node's scale. | https://github.com/godotengine/godot/pull/112184 [done] | (update in existing section 3. ParticleProcessMaterial — Essential Properties) |
| particles-vfx | api | Per-axis 3D scale and rotation — `ParticleProcessMaterial` gains `use_scale_3d` + `scale_3d_min/max` (`Vector3`) and `use_rotation_3d` + `rotation_3d_min/max` (`Vector3`, degrees; 3D only). | https://github.com/godotengine/godot/pull/112447 [done] | (update in existing section 3. ParticleProcessMaterial — Essential Properties) |
| particles-vfx | api | Particle orientation rework — `GPUParticles3D` gains `TRANSFORM_ALIGN_LOCAL_BILLBOARD = 4` (face camera while preserving a given axis), `transform_align_axis` (X or Y), and `transform_align_channel_filter`; `ParticleProcessMaterial` gains `use_rotation_velocity_3d` + `rotation_velocity_3d_min/max/curve` (per-axis rotation velocity over lifetime). | https://github.com/godotengine/godot/pull/116620 [done] | (update in existing section 2. Key Node Properties) |
| particles-vfx | api | Particle seeking — `GPUParticles2D/3D` (and CPU variants) `request_particles_process()` gains an optional `process_time_residual: float = 0` parameter for sub-frame-accurate pre-simulation: `request_particles_process(process_time, process_time_residual = 0)`. | migration guide (GH-109142) [done] | (update in existing section 10. Performance Tips) |
| particles-vfx | fix | Subemitted particles now inherit parent velocity — `sub_emitter_keep_velocity` (default `false`, verified in class reference) previously did not account for the parent particle's velocity; 4.7 fixes it, so existing subemitter effects may look different. | https://github.com/godotengine/godot/pull/118062 [done] | (warning in existing section 6. Subemitters) |
| xr-development | api | User presence (`XR_EXT_user_presence`) — `OpenXRInterface` gains the `user_presence_changed(is_user_present: bool)` signal and `is_user_presence_supported()`; not emitted during startup/shutdown (assume presence gained on startup, lost on shutdown). | https://github.com/godotengine/godot/pull/115190 [done] | (new section: Godot 4.7+ XR Features) |
| xr-development | api | Composition layer eye visibility — `OpenXRCompositionLayer.eye_visibility` (`EyeVisibility` enum: `EYE_VISIBILITY_BOTH = 0` default, `EYE_VISIBILITY_LEFT = 1`, `EYE_VISIBILITY_RIGHT = 2`) renders a layer to one eye only. | https://github.com/godotengine/godot/pull/113096 [done] | (new section: Godot 4.7+ XR Features) |
| xr-development | api | Spatial anchors extensibility — `OpenXRSpatialAnchorCapability.create_new_anchor()` gains an optional `next: OpenXRStructureBase = null` parameter (signature: `create_new_anchor(transform: Transform3D, spatial_context: RID = RID(), next: OpenXRStructureBase = null)`). | migration guide (GH-118128) [done] | (new section: Godot 4.7+ XR Features) |
| xr-development | warn | Foveation defaults — new project setting `xr/openxr/foveation_eye_tracked = true` (eye-tracked foveation enabled by default where supported) and `xr/openxr/foveation_with_subsampled_images = true` (subsampled images now on by default). | https://github.com/godotengine/godot/pull/117868 [done] | (update in existing section 7. Meta Quest Export) |
| mobile-development | api | Implement Java interfaces from GDScript — `JavaClassWrapper.create_proxy(object: Object, interfaces: PackedStringArray) -> JavaObject` (the Godot object's method signatures must match the Java interfaces) and `create_sam_callback(sam_interface: String, callable: Callable) -> JavaObject` for single-abstract-method interfaces. | https://github.com/godotengine/godot/pull/115498 [done] | (update in existing section 4. Calling Android APIs (JavaClassWrapper) — Godot 4.4+) |
| mobile-development | api | `VirtualJoystick` Control node for touch input (see input-handling row for API details) — replaces hand-rolled on-screen joysticks on mobile. | https://github.com/godotengine/godot/pull/110933 [done] | (update in existing section 5. Device features & safe area, cross-ref input-handling) |
| mobile-development | api | Picture-in-picture (Android) — `DisplayServer.pip_mode_enter(window_id = 0)`, `is_in_pip_mode(window_id = 0)`, `pip_mode_set_aspect_ratio(numerator, denominator, window_id = 0)`, `pip_mode_set_auto_enter_on_background(bool, window_id = 0)`; `Node.NOTIFICATION_APPLICATION_PIP_MODE_ENTERED = 2019` / `NOTIFICATION_APPLICATION_PIP_MODE_EXITED = 2020`. | https://github.com/godotengine/godot/pull/114505 [done] | (update in existing section 2. App lifecycle) |
| mobile-development | api | Device orientation signal — `DisplayServer.orientation_changed(orientation: int)`: `1` portrait, `2` landscape, `0` undefined. | https://github.com/godotengine/godot/pull/115434 [done] | (update in existing section 5. Device features & safe area) |
| mobile-development | fix | Native Android file picker now enabled on all devices — the Android version check gating `FEATURE_NATIVE_DIALOG_FILE` support was removed (platform item; no new script API). | https://github.com/godotengine/godot/pull/115257 [done] | (update in existing section 5. Device features & safe area) |
| mobile-development | fix | Android "Use Gradle Build" export option is no longer marked experimental (editor/export item). | https://github.com/godotengine/godot/pull/119172 [done] | (update in existing section 1. Export & signing) |
| mobile-development | warn | Deprecated Google Play OBB expansion-file support removed from the Android export (export item) — projects still relying on APK expansion must migrate to Play Asset Delivery or PCK patching. | https://github.com/godotengine/godot/pull/118283 [done] | (warning in existing section 1. Export & signing) |
| ai-navigation | fix | `NavigationServer3D.map_get_closest_point_normal()` now returns a normalized vector (previously could be unnormalized) — code that compensated by normalizing manually keeps working; code relying on the unnormalized magnitude breaks. Method signature verified in class reference; the normalization change itself is changelog-cited. | https://github.com/godotengine/godot/pull/119022 [done] | (warning in existing section 9. Common Pitfalls) |
| multithreading | fix | Threaded resource loading reliability — 4.7 fixes several `ResourceLoader` threaded-load defects: `load_threaded_get()` deadlocks (GH-119757, GH-120077), a race in `load_threaded_request()` (GH-118824), and resources from `load_threaded_get()` never being unloaded (GH-119394). Behavior-only; no API change — guidance can drop pre-4.7 deadlock caveats. | https://github.com/godotengine/godot/pull/119757 [done] | (update in existing section 5. Threaded resource loading) |
| math-essentials | api | `Basis.is_orthonormal()` (const) — returns whether the basis is orthonormal (orthogonal axes of unit length); complements existing `orthonormalized()`. | https://github.com/godotengine/godot/pull/117206 [done] | (update in existing section 2. Transforms) |
| save-load | warn | `JSON.stringify()` now serializes an empty `Dictionary` compactly (`{}`) even when an `indent` is passed — save files written with an indent change formatting for empty-dict fields; anything diffing/hashing save output must tolerate it. `stringify(data, indent = "", sort_keys = true, full_precision = false)` verified in class reference; the compaction change itself is changelog-cited. | https://github.com/godotengine/godot/pull/115883 [done] | (warning in existing section 3. JSON — Game Saves) |
| godot-ui | warn | AccessibilityServer split — `Control.accessibility_live` changes type from `DisplayServer.AccessibilityLiveMode` to `AccessibilityServer.AccessibilityLiveMode` (`LIVE_OFF = 0` default, `LIVE_POLITE = 1`, `LIVE_ASSERTIVE = 2`); accessibility enums/APIs now live on the new `AccessibilityServer` singleton (inherits Object). GDScript-compatible; breaks C# binary and source compatibility. | migration guide (GH-116839) [done] | (warning in existing section 1. Control Node Hierarchy) |
| godot-ui | api | `Control.custom_maximum_size = Vector2(-1, -1)` — per-axis maximum size that has priority over `custom_minimum_size`; comes with `propagate_maximum_size = false` (parent max size constrains Control children) and the `_get_maximum_size()` virtual for code-driven maximums. | https://github.com/godotengine/godot/pull/116640 [done] | (update in existing section 2. Common Container Nodes) |
| godot-ui | api | Control offset transform — visual transform applied on top of layout without moving the layout rect: `offset_transform_enabled = false`, `offset_transform_position` / `offset_transform_position_ratio` (`Vector2(0, 0)`), `offset_transform_rotation = 0.0`, `offset_transform_scale = Vector2(1, 1)`, `offset_transform_pivot` + `offset_transform_pivot_ratio = Vector2(0.5, 0.5)`, `offset_transform_visual_only = true`. Ideal for UI juice (shake/pulse) that must not re-trigger container layout. | https://github.com/godotengine/godot/pull/87081 [done] | (new subsection in existing section 6. Common UI Patterns) |
| godot-ui | api | `Control._get_cursor_shape(at_position: Vector2) -> int` virtual (const) — return a per-position cursor shape while hovering the control; used by `get_cursor_shape()`, complements the static `mouse_default_cursor_shape`. | https://github.com/godotengine/godot/pull/111819 [done] | (update in existing section 6. Common UI Patterns) |
| godot-ui | api | PopupMenu search bar — `search_bar_enabled = false`, `search_bar_fuzzy_search_enabled = true`, `search_bar_fuzzy_search_max_misses = 2`, `search_bar_min_item_count = 0` (minimum items before the bar shows), plus `set_item_index(index, target_index)` for reordering items. | https://github.com/godotengine/godot/pull/114236 [done] | (update in existing section 6. Common UI Patterns) |
| godot-ui | api | Conic gradients — `GradientTexture2D` `Fill` enum gains `FILL_CONIC = 3` (angular sweep around `fill_from`), joining `FILL_LINEAR = 0`, `FILL_RADIAL = 1`, `FILL_SQUARE = 2`. Useful for radial progress/cooldown indicators without a shader. | https://github.com/godotengine/godot/pull/115394 [done] | (update in existing section 4. Theme System) |
| godot-ui | fix | `TextureRect` `STRETCH_TILE` now supports `AtlasTexture` textures — the class reference limits the restriction to `AtlasTexture` with non-zero `margin` only (previously tiling an AtlasTexture was unsupported outright). | https://github.com/godotengine/godot/pull/113808 [done] | (update in existing section 6. Common UI Patterns) |
| godot-ui | warn | RichTextLabel image sizing rework — `add_image()` / `update_image()` `width`/`height` change `int` → `float`, and the `width_in_percent`/`height_in_percent` bool params are renamed `width_unit`/`height_unit` typed as the new `ImageUnit` enum (`IMAGE_UNIT_PIXEL = 0`, `IMAGE_UNIT_PERCENT = 1`, `IMAGE_UNIT_EM = 2` — scale relative to font size); enum field `ImageUpdateMask.UPDATE_WIDTH_IN_PERCENT` is renamed `UPDATE_WIDTH_UNIT` (GDScript-breaking). | migration guide (GH-112617) [done] | (warning in existing section 6. Common UI Patterns) |
| godot-ui | warn | `TabContainer.all_tabs_in_front` deprecated — class reference: "Due to internal changes this doesn't do anything anymore, as they're always in front." Remove any guidance that sets it. | https://github.com/godotengine/godot/pull/118623 [done] | (update in existing section 2. Common Container Nodes) |
| responsive-ui | fix | New-project stretch defaults — newly created 4.7 projects default `display/window/stretch/mode` to `canvas_items` and `display/window/stretch/aspect` to `expand` (previously `disabled`/`keep`); the property defaults for existing projects are unchanged (`"disabled"` in the class reference). Skill text recommending these values can now note they are the 4.7 defaults. | migration guide (GH-118815) [done] | (update in existing section 1. Project Settings for Resolution) |
| godot-project-setup | fix | Same new-project stretch default change as responsive-ui (`canvas_items`/`expand` for newly created projects, GH-118815) — the project settings scaffold should mention that 4.7 already sets these. | migration guide (GH-118815) [done] | (update in existing section Project Settings) |
| 3d-essentials | api | `AreaLight3D` — new rectangle-shaped `Light3D` node: `area_size = Vector2(1, 1)`, `area_range = 5.0`, `area_attenuation = 1.0` (`2.0` = physically accurate inverse square), `area_normalize_energy = true`, optional `area_texture` (textured emission, Forward+/Mobile only); PCSS soft shadows. Limited Mobile support; cannot cast shadows in Compatibility; higher GPU cost than omni/spot (clustered-lighting cost on all objects in Forward+). | https://github.com/godotengine/godot/pull/108219 [done] | (update in existing section 3. Lighting + references/materials-and-lighting-recipes.md) |
| 3d-essentials | api | GridMap octant querying — `cell_octant_size = 8` plus `get_used_octants()`, `get_used_octants_by_item(item)`, `get_octants_in_bounds(bounds: AABB)`, `get_used_octants_in_bounds(bounds)`, `get_used_cells_in_octant(octant_coords)`, `get_used_cells_in_octant_by_item(octant_coords, item)`, `get_octant_coords_from_cell_coords(cell_coords)` (all return `Vector3i`/arrays of `Vector3i`). | https://github.com/godotengine/godot/pull/118280 [done] | (update in existing section 1. 3D Coordinate System & Core Nodes) |
| 3d-essentials | api | CSG automatic smoothing — `CSGShape3D` gains `autosmooth = false` and `smoothing_angle = 50.0`; with autosmooth enabled, normals are computed from the angle between faces (class reference: angles greater than `smoothing_angle` are smoothed, smaller stay sharp; below `0.1` disables smoothing for performance). | https://github.com/godotengine/godot/pull/116749 [done] | (update in existing section 1. 3D Coordinate System & Core Nodes) |
| 3d-essentials | warn | Volumetric fog blending rework — fog is now blended using transmittance instead of opacity in the early-out branch, so existing volumetric fog can look different after upgrade; new project setting `rendering/environment/fog/use_legacy_blending = false` restores the pre-4.7 behavior. | https://github.com/godotengine/godot/pull/119414 [done] | (warning in existing section 6. Fog + references/fog-recipes.md) |
| 3d-essentials | warn | `rendering/reflections/sky_reflections/roughness_layers` default changed `7` → `8` — sky-reflection roughness mip distribution changes for projects that left the setting at default; set it back to `7` to keep 4.6 output. | migration guide (GH-116154) [done] | (warning in existing section 4. Environment & Post-Processing) |
| 3d-essentials | api | HDR output — `display/window/hdr/request_hdr_output = false` (verified in ProjectSettings class reference) requests HDR display output where supported; in 4.7 it is promoted from an advanced to a basic project setting. | https://github.com/godotengine/godot/pull/118355 [done] | (update in existing section 4. Environment & Post-Processing) |
| 3d-essentials | api | Vulkan raytracing (RenderingDevice BLAS/TLAS/RT pipelines) shipped experimental in 4.7 — mention-only, no recipes until stabilized. | https://github.com/godotengine/godot/pull/99119 [done] | (one-line note in rendering section) |
| 2d-essentials | warn | `CanvasItem` antialiased line drawing no longer adds the antialiasing feather — the feather made `draw_line()`-style lines appear thicker than intended; after upgrade, antialiased lines render thinner and projects relying on the old look must draw a thicker `width`. | migration guide (GH-105122) [done] | (warning in existing section 8. 2D Antialiasing) |
| 2d-essentials | api | DrawableTexture2D — runtime-drawable texture type, experimental in 4.7 — mention-only. | https://github.com/godotengine/godot/pull/105701 [done] | (one-line note in custom drawing section) |
| shader-basics | api | New visual shader inputs — `in_shadow_pass` (spatial vertex/fragment; maps to the `IN_SHADOW_PASS` built-in, `true` while rendering a shadow-mapping pass) and `specular_amount` (spatial `light()`; maps to `SPECULAR_AMOUNT`, GH-115325). Built-in names verified in the spatial shader reference; the visual-shader input exposure is editor-side and changelog-cited. | https://github.com/godotengine/godot/pull/115326 [done] | (update in existing section 5. Visual Shaders) |
| shader-basics | warn | `LinearToSRGB` visual shader node no longer clamps its output to `[0.0, 1.0]` when using the Forward+ or Mobile renderer — HDR values now pass through; graphs relying on the implicit clamp produce different output. | migration guide (GH-113956) [done] | (warning in existing section 5. Visual Shaders) |
| shader-basics | warn | `textureQueryLod()` is fragment-shader-only — the shader-functions reference notes "Available only in the fragment shader", and 4.7 enforces this with a compile error; shaders calling it from `vertex()` stop compiling after upgrade. | https://github.com/godotengine/godot/pull/118962 [done] | (warning in existing section 12. Common Pitfalls) |
| addon-development | api | Unsaved-state and script-editor control APIs — `EditorInterface.get_unsaved_scenes() -> PackedStringArray`, plus `ScriptEditor.get_unsaved_files() -> PackedStringArray`, `ScriptEditor.close_file(path: String) -> Error`, `ScriptEditor.save_all_scripts()`, and `ScriptEditor.reload_open_files()` (GH-113767, GH-113762, GH-113772, GH-113765, GH-116187). All verified in the class reference. | https://github.com/godotengine/godot/pull/113767 [done] | (update in existing section 3. EditorPlugin Base) |
| addon-development | api | `EditorInspector.create_default_inspector(filter_line_edit: LineEdit = null) -> EditorInspector` (static) — instantiate a fully configured default inspector for use inside plugin UIs. | https://github.com/godotengine/godot/pull/118301 [done] | (update in existing section 4. Custom Inspector Plugin + references/inspector-plugins.md) |
| addon-development | api | `EditorContextMenuPlugin` gains `CONTEXT_SLOT_INSPECTOR_PROPERTY = 7` in the `ContextMenuSlot` enum — context-menu plugins can now extend the inspector property right-click menu. | https://github.com/godotengine/godot/pull/118187 [done] | (update in existing section 4. Custom Inspector Plugin) |
| addon-development | api | `EditorNode3DGizmoPlugin._can_commit_handle_on_click() -> bool` virtual (const) — lets a gizmo plugin opt into committing a handle action on a plain click. | https://github.com/godotengine/godot/pull/115911 [done] | (update in existing section 7. Gizmos + references/gizmos-deep-dive.md) |
| csharp-godot | warn | 4.7 C# compatibility breaks (per the migration guide's breaking-changes tables): binary-incompatible — `OptimizedTranslation.Generate()` (void → bool, GH-119563), `Control.AccessibilityLive` (enum type moved to `AccessibilityServer`, GH-116839), `Animation.Length` (float → double type metadata, GH-116394); source-incompatible — `RichTextLabel.ImageUpdateMask.UpdateWidthInPercent` renamed `UpdateWidthUnit` (GH-112617), `EditorSceneFormatImporter` IMPORT_* constants moved into the `ImportFlags` enum (GH-115788); removed — `AudioEffectSpectrumAnalyzer.TapBackPos` (GH-114355). Rebuild against 4.7 and fix renamed symbols. | migration guide (breaking-changes compatibility tables) [done] | (warning in existing section 9. Common Gotchas) |
| csharp-godot | api | Godot.NET preview opt-in — the `Godot.NET.Sdk` MSBuild property `EnableGodotDotNetPreview` (set to `true` in the .csproj) switches a project to the preview Godot.NET bindings (`Godot.Bindings` instead of `GodotSharp`); property name verified in the engine's `Godot.NET.Sdk` targets. Editor/buildsystem item. | https://github.com/godotengine/godot/pull/118001 [done] | (update in existing section 2. Project Setup) |
| gdextension | warn | `Object.is_class(class: StringName)` — parameter type changed from `String` to `StringName` for performance; GDScript-compatible and a C# compat method was added, but GDExtension bindings compiled against the old signature rely on the compatibility mapping. Signature verified in class reference. | migration guide (GH-118582) [done] | (warning in existing section 5. Compatibility rules) |
| gdextension | api | Refcount-aware init entry points — GDExtension interface adds `classdb_construct_object3` and `classdb_register_extension_class6`, making construction/registration refcount-aware (native interface surface; changelog-cited, no class-reference surface). | https://github.com/godotengine/godot/pull/118214 [done] | (update in existing section 4. Entry point & the .gdextension file) |
| gdextension | warn | GDExtension interface functions `object_cast_to` and `classdb_get_class_tag` are deprecated in favor of `is_class`-based casts — native code should migrate its cast paths (native interface surface; changelog-cited). | https://github.com/godotengine/godot/pull/119254 [done] | (update in existing section 3. Binding a class (C++)) |
| gdextension | fix | Reference GDExtension files moved from godot-cpp to godot-headers — upstream guidance now points at the godot-headers repo for the interface headers (tooling item). | https://github.com/godotengine/godot/pull/115401 [done] | (update in existing section 2. Project & build setup) |
| gdextension | fix | TextServer GDExtension build support removed — custom text servers can no longer be built as GDExtensions in 4.7 (buildsystem item); drop any suggestion that text servers are a GDExtension use case. | https://github.com/godotengine/godot/pull/117056 [done] | (update in existing section 1. When to reach for GDExtension) |
| localization | api | `EditorTranslationParserPlugin._customize_strings(strings: Array[PackedStringArray]) -> Array[PackedStringArray]` virtual (const) — plugins can directly modify the final list of extracted strings during POT generation. | https://github.com/godotengine/godot/pull/108909 [done] | (update in existing section 2. Translation Files) |
| localization | warn | `OptimizedTranslation.generate(from: Translation)` return type changed `void` → `bool` — now reports success; GDScript-compatible but binary-incompatible for C#. Verified in class reference. | migration guide (GH-119563) [done] | (update in existing section 2. Translation Files) |
| localization | api | `Control.translation_context: StringName = &""` — per-control translation context for disambiguating identical source strings, matching the context argument of `tr()`. Verified in class reference. | https://github.com/godotengine/godot/pull/115340 [done] | (update in existing section 3. Using tr() in Code) |
| localization | fix | POT generator now extracts `Control.accessibility_name` / `accessibility_description` strings — accessibility strings become translatable without manual listing (editor/tooling item). | https://github.com/godotengine/godot/pull/117134 [done] | (update in existing section 2. Translation Files) |
| assets-pipeline | api | 3D scene files can now be imported as a single `Mesh` or as a `MeshLibrary` via ResourceImporterScene's import-type option (editor Import dock item; changelog-cited, no new script API). | https://github.com/godotengine/godot/pull/107856 [done] | (update in existing section 3. 3D Scene Import) |
| assets-pipeline | api | `GLTFDocument` `ImportFlags` enum exposed — `IMPORT_FLAG_GENERATE_TANGENT_ARRAYS = 8`, `IMPORT_FLAG_USE_NAMED_SKIN_BINDS = 16`, `IMPORT_FLAG_DISCARD_MESHES_AND_MATERIALS = 32`, `IMPORT_FLAG_FORCE_DISABLE_MESH_COMPRESSION = 64` — with a `flags: int = 0` parameter on `append_from_file()`, `append_from_buffer()`, and `append_from_scene()`. | https://github.com/godotengine/godot/pull/115788 [done] | (update in existing section 3. 3D Scene Import) |
| assets-pipeline | warn | `EditorSceneFormatImporter` constants `IMPORT_SCENE`, `IMPORT_ANIMATION`, `IMPORT_FAIL_ON_MISSING_DEPENDENCIES`, `IMPORT_GENERATE_TANGENT_ARRAYS`, `IMPORT_USE_NAMED_SKIN_BINDS`, `IMPORT_DISCARD_MESHES_AND_MATERIALS`, `IMPORT_FORCE_DISABLE_MESH_COMPRESSION` moved into a new `ImportFlags` enum (bitfield) — GDScript-compatible, C# source-incompatible. | migration guide (GH-115788) [done] | (warning in existing section 3. 3D Scene Import) |
| assets-pipeline | warn | Font import `hinting` default changed `1` (Light) → `3` (Light (Except Pixel Fonts)) on `ResourceImporterDynamicFont` — pixel-style fonts now auto-disable hinting on import; re-set `hinting = 1` per font to keep 4.6 rendering. Values verified in class reference (`0` None, `1` Light, `2` Normal, `3`/`4` Light/Normal except pixel fonts). | migration guide (GH-115372) [done] | (warning in existing section 7. Common Pitfalls) |
| assets-pipeline | api | DDS import gains R8 and R8G8 texture format support (editor import item; changelog-cited). | https://github.com/godotengine/godot/pull/116307 [done] | (update in existing section 2. Image Import) |
| export-pipeline | api | Patch PCKs on Android — an APK/AAB can now be provided as the base pack when exporting patch PCKs, enabling PCK-patching workflows for Android builds (export tooling item; changelog-cited). | https://github.com/godotengine/godot/pull/116553 [done] | (update in existing section 3. Export from CLI) |
| export-pipeline | api | SparsePCK index encryption — sparse PCK exports support encrypting the file index (export tooling item; changelog-cited), complementing existing PCK encryption options. | https://github.com/godotengine/godot/pull/113920 [done] | (update in existing section 1. Export Presets) |
| export-pipeline | api | `PCKPacker.add_file_from_buffer(target_path: String, data: PackedByteArray, encrypt: bool = false) -> Error` — pack in-memory data into a PCK without writing a temp file. Verified in class reference. | https://github.com/godotengine/godot/pull/108830 [done] | (update in existing section 3. Export from CLI) |
| export-pipeline | fix | The "runnable" flag moved out of export presets (`runnable=` is no longer a per-preset property in `export_presets.cfg`) — the skill's preset example showing `runnable=true` is stale for 4.7 projects. | https://github.com/godotengine/godot/pull/114930 [done] | (update in existing section 1. Export Presets) |
| physics-system | fix | Carry-forward stale text: §8 (Jolt) carried a "Godot 4.6 is in beta; verify behavior on stable release" note — 4.6 has long been stable (engine changelog links the `4.6-stable` tag) and 4.7 is out. Note removed; the surrounding text already states the grounded facts (Jolt default for new 3D projects since 4.4, non-experimental since 4.6). | engine changelog "Past releases" (4.6-stable tag) [done] | (fix in existing section 8. Jolt Physics) |
| localization | fix | Carry-forward stale text: the same "Godot 4.6 is in beta; verify behavior on stable release" note was moved verbatim into `references/csv-plural-context.md` during Task 17. Note removed identically; the document's title already carries the "(Godot 4.6+)" requirement. | engine changelog "Past releases" (4.6-stable tag) [done] | (fix in references/csv-plural-context.md) |

## Dropped candidates (Task 3)

- **godot-ui / BaseButton multitouch support (GH-110893)** — [dropped: behavior-only change with no class-reference property/method surface and no migration-guide entry; fails the verification rule for runtime behavior changes]
- **shader-basics / struct name usable as a variable name (GH-116888)** — [dropped: shader-language parser relaxation with no class-reference or shader-reference doc surface]
- **shader-basics / `textureGather` restrictions in Compatibility (GH-117157)** — [dropped: no restriction note in the shader-functions reference and no migration-guide entry; runtime shader behavior needs doc backing]
- **csharp-godot / `Godot.Collections.Array(<T>).AddRange(ReadOnlySpan<T>)` (GH-106765)** — [dropped: C# binding API with no class-reference or migration-guide surface; the changelog-alone exception is limited to editor/tooling/platform items]
- **csharp-signals / source-generated `EmitSignal` performance improvement (GH-115741)** — [dropped: internal optimization with no API or documented behavior surface; csharp-signals therefore intentionally has zero 4.7 rows]
- **addon-development / `EditorDock` class + `EditorPlugin.add_dock()` (spec touchpoint)** — [dropped: not a 4.7 delta — EditorDock was introduced in Godot 4.6 (engine commit 97b398cba1 "Rework editor docks", contained in tag 4.6-stable); the 4.7 changelog only contains conversions of built-in docks to it]
- **save-load–export / `ZIPPacker.start_file()` new `permissions`/`modified_time` params (GH-115946)** — [dropped: verified in class reference, but no skill references ZIPPacker; the spec conditioned inclusion on the inventory confirming a skill touchpoint]

## Dropped candidates (Task 18)

- **xr-development / OpenXR `XR_KHR_generic_controller` support (GH-110778)** — [dropped: no class-reference or settings surface]
- **mobile-development / C# `NotificationWMGoBackRequest` → `NotificationWmGoBackRequest` casing "fix" (carry-forward item)** — [dropped: the existing `NotificationWMGoBackRequest` spelling is correct — GodotSharp's bindings generator hardcodes a `"WM"` → `"WM"` part override in `modules/mono/utils/naming_utils.cpp` (`pascal_case_part_overrides`), GodotTools' own C# source uses `Node.NotificationWMWindowFocusIn`, and the official quit-request tutorial uses `NotificationWMCloseRequest`; applying the proposed lowercase-`Wm` rename would have introduced a compile error]
